# ax-portfolio · 절대 규칙

AX 포트폴리오 특강용 사전 제작 사이트 저장소. 클로드 코드는 오케스트레이터, Antigravity CLI(agy)는 집필자다.

1. 프롬프트는 정본 노트(넥서스 `8ffcf001`)의 코드 블록 안 글자를 그대로 파일로 옮긴다. 고쳐 쓰지 않는다.
2. agy는 자기 폴더 `sites/<폴더>/index.html` 하나만 만든다. git 명령 금지. 다른 폴더·루트 수정 금지.
3. 커밋과 push는 오케스트레이터만, 모든 판정이 끝난 뒤 한 번만 한다.
4. 최유나(`choi-yuna`)는 3교시 라이브용이다. `prompts/choi-yuna/` 파일은 옮기되 사이트는 만들지 않는다.
5. 보고와 기록에서 항목을 번호·부호로만 부르지 않는다. 이름으로 부른다.
6. 의존하지 않는 명령과 도구 호출은 한 응답에 함께 던진다.
